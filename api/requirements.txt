# Pinning major versions only — Vercel resolves the latest patch.
# akshare and PyQt6 from the original ForeSight requirements are
# intentionally omitted: akshare's quant backend gracefully degrades
# (try/except wraps the import) and PyQt6 was only for the desktop GUI.
requests>=2.28
pyyaml>=6.0
